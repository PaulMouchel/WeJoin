<p id="notice"><%= notice %></p>

<h1>Attendances</h1>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @attendances.each do |attendance| %>
      <tr>
        <td><%= attendance.date %></td>
        <td><%= link_to 'Supprimer', user_attendance_path(current_user, attendance), method: :delete, data: { confirm: 'Es-tu sûr ?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

