
<div class="container">
	<div class="row justify-content-center">
		<div class="col-lg-12">
			<div class="card-place mt-5">
				<a class="prev" onclick="plusSlides(-1)">&#10094;</a>
				<a class="next" onclick="plusSlides(1)">&#10095;</a>
				<% count = 1 %>
				<%if @place.place_pics.attached?%>
					<% @place.place_pics.each do |pic| %>
						<div class="mySlides">
							<%= image_tag pic, class:"img-place", style:"width:100%" %>
						</div>
						<% count += 1 %>
					<% end %>
				<%else%>
					<p>=== Il n'y a pas encore de photo lié à cet endroit ===</p>
				<%end%>
			</div>
		</div>	
		<div class="col-lg-12">	

		  	<div class="card-body">
			  	<div id="place-div-<%=@place.id%>" class="position_favorite">
					<%= render "favorite_place_icon", place: @place %>
				</div>
				<%= render 'show_places' %>	
			</div>

			<div class="text-center">
				<%= link_to 'Proposer une modification', new_city_place_place_edition_path(@city, @place), class:'btn-submit' %>
				<span><%= link_to 'Back', city_places_path(@city), class:'btn-back' %></span>
			</div>
		</div>
	</div>
</div>	
<div class="space"></div>
<script>
var slideIndex = 1;
showSlides(slideIndex);

function plusSlides(n) {
  showSlides(slideIndex += n);
}

function currentSlide(n) {
  showSlides(slideIndex = n);
}

function showSlides(n) {
  var i;
  var slides = document.getElementsByClassName("mySlides");
  if (n > slides.length) {slideIndex = 1}    
  if (n < 1) {slideIndex = slides.length}
  for (i = 0; i < slides.length; i++) {
      slides[i].style.display = "none";  
  }
  slides[slideIndex-1].style.display = "block";  
}
</script>

 <script src="https://unpkg.com/leaflet@1.5.1/dist/leaflet.js" integrity="sha512-GffPMF3RvMeYyc1LWMHtK8EbPv0iNZ8/oTtHPx9/cc2ILxQ+u905qIwdpULaqDkyBKgOaB57QTMg7ztg8Jm2Og==" crossorigin=""></script>
	 
	<script>
	var city = [<%= @place.city.latitude %>, <%= @place.city.longitude %>]
	 var mymap = L.map('map_place').setView(city, 16);

	L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
			attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
	}).addTo(mymap);		
	
	marker = L.marker([<%= @place.latitude %>, <%= @place.longitude %>]).addTo(mymap);

	marker.bindPopup("<b><%= @place.name %></b><br><%= @place.address %>").openPopup();

	</script>

