myStarsArray = [""];
avStarsArray = [""];
for (i=1 ; i<=5 ; i++) {
	myStarsArray.push(document.getElementById("my-noise-" + i));
	avStarsArray.push(document.getElementById("av-noise-" + i));
}

myStars = <%= @rating.noise %>;
avStars = <%= @rating.place.average_rating_noises %>;

actualizeStars(myStarsArray, myStars);
actualizeStars(avStarsArray, Math.trunc(avStars));

document.getElementById("my-noise-num").textContent = myStars;
document.getElementById("av-noise-num").textContent = avStars.toFixed(1);

function actualizeStars(stars, rating) {
	for (i=1 ; i<=rating ; i++) {
		stars[i].classList.add("fas");
		stars[i].classList.remove("far");
	}
	for (i=rating+1 ; i<=5 ; i++) {
		stars[i].classList.add("far");
		stars[i].classList.remove("fas");
	}
}