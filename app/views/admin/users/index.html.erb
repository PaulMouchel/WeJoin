  <section class="banner">
    <div class="container">
      <div class="breadcrumb-banner d-flex flex-wrap align-items-center justify-content-end">
        <div class="col-first">
          <h1>List users</h1>
          <nav class="d-flex align-items-center">
            <a href="<%= admin_cities_path %>">Menu Admin<span class="lnr lnr-arrow-right"></span></a>
          </nav>
        </div>
      </div>
    </div>
  </section>

  
<div class="space"></div>


<div class="container">
  <div class="row">
    <div class="col-lg-12">
      
 
<table class="table">
  <thead>
    <tr class="table-info">
      <th>id</th>
      <th>Email</th>
      <th>Description</th>
      <th>First name</th>
      <th>Last name</th>
      <th>Admin?</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.sort_by{ |user| user.email }.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.email %></td>
        <td><%= user.description %></td>
        <td><%= user.first_name %></td>
        <td><%= user.last_name %></td>
        <td><span class="bg-<% if user.is_admin %>success<% else %>danger<% end %> rounded-circle", style="height: 20px; width: 20px; display: inline-block;"></span></td>
        <td><%= link_to 'Show', admin_user_path(user), class:"btn btn-info" %></td>
        <td><%= link_to 'Edit', edit_admin_user_path(user), class:"btn btn-primary" %></td>
        <td><%= link_to 'Destroy', admin_user_path(user), method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

    </div>
  </div>
</div> 

<div class="space"></div>